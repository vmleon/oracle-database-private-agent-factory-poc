# Cloud deployment (OCI)

Not yet implemented. The cloud topology (5 computes + **ADB** + **LB**), the Terraform layout, and the Ansible roles are designed in [`docs/DEPLOYMENT.md §4`](docs/DEPLOYMENT.md). Infrastructure-as-code lands under `deploy/tf/` and `deploy/ansible/` when this is built.

For the working local runbook, see [`LOCAL.md`](LOCAL.md).
