databaseChangeLog:
  # BANK_VIEWS.* — the NL2SQL surface for the two PAF agents.
  #
  # Design model (see DESIGN.md §6.2 / §10 / USE-CASE.md):
  # - chat_v_* (customer-safe): exposed to CHAT_AGENT via the
  #   `chat_profile` Select AI profile. Aggregated transactions, latest
  #   bureau snapshot, own facilities. NO access to other customers, NO
  #   protected attributes, NO decision_audit, NO parameter history.
  # - research_v_* (broader, read-only): exposed to RESEARCH_AGENT via
  #   the `research_profile` Select AI profile. Row-level transactions,
  #   decision history, decision_audit, research_audit, parameter
  #   history, hitl_task. Read-only by view definition (no INSTEAD OF
  #   triggers) and by RESEARCH_AGENT having no write tools.
  #
  # Both view sets live in the BANK_VIEWS schema so the agent's
  # NL2SQL object list pins to BANK_VIEWS.* and can never touch a base
  # table directly.
  #
  # The customer scope is enforced by the Application Service threading
  # a customer_id parameter into every CHAT_AGENT invocation — views
  # themselves are not row-filtered. Production would add VPD / RLS;
  # out of scope for this PoC.

  # BANK_VIEWS owns the views; it needs SELECT on the underlying BANK_CORE
  # tables. Without these grants, the views compile but querying them
  # as PAF_PLATFORM fails with ORA-01031 (Oracle resolves the view to
  # base tables and checks the owner's — BANK_VIEWS's — privileges).
  - changeSet:
      id: 006-grant-app-to-reporting
      author: paf-poc
      changes:
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.customer TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.employment TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.account TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.account_transaction TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.credit_bureau_snapshot TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.existing_facility TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.loan_application TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.product_catalog TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.loan_application_document TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.decision TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.decision_audit TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.research_audit TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.policy_parameter_history TO BANK_VIEWS
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.hitl_task TO BANK_VIEWS

  # Customer-safe views (CHAT_AGENT)

  - changeSet:
      id: 006-create-chat-applicant-profile
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.chat_v_applicant_profile AS
              SELECT
                c.customer_id,
                c.full_name,
                c.date_of_birth,
                TRUNC(MONTHS_BETWEEN(SYSDATE, c.date_of_birth) / 12) AS age_years,
                c.residency,
                c.kyc_status,
                e.employer_name,
                e.employment_type,
                e.monthly_salary,
                e.tenure_months
              FROM BANK_CORE.customer c
              LEFT JOIN BANK_CORE.employment e
                ON e.customer_id = c.customer_id
                AND e.is_current = 'Y'

  - changeSet:
      id: 006-create-chat-transactions-summary
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.chat_v_transactions_summary AS
              SELECT
                a.customer_id,
                TRUNC(t.txn_date, 'MM') AS month,
                t.txn_type,
                COUNT(*) AS txn_count,
                SUM(t.amount) AS net_amount,
                SUM(CASE WHEN t.amount > 0 THEN t.amount ELSE 0 END) AS credits,
                SUM(CASE WHEN t.amount < 0 THEN -t.amount ELSE 0 END) AS debits
              FROM BANK_CORE.account_transaction t
              JOIN BANK_CORE.account a ON a.account_id = t.account_id
              GROUP BY a.customer_id, TRUNC(t.txn_date, 'MM'), t.txn_type

  - changeSet:
      id: 006-create-chat-credit-bureau
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.chat_v_credit_bureau AS
              SELECT
                s.customer_id,
                s.snapshot_date,
                s.score,
                s.score_scale_min,
                s.score_scale_max,
                s.bureau_name,
                s.total_debt,
                s.num_open_facilities,
                s.num_late_payments_12m
              FROM BANK_CORE.credit_bureau_snapshot s
              WHERE s.snapshot_date = (
                SELECT MAX(s2.snapshot_date)
                  FROM BANK_CORE.credit_bureau_snapshot s2
                 WHERE s2.customer_id = s.customer_id
              )

  - changeSet:
      id: 006-create-chat-existing-facilities
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.chat_v_existing_facilities AS
              SELECT
                f.customer_id,
                f.lender_name,
                f.facility_type,
                f.balance,
                f.monthly_payment,
                f.opened_at
              FROM BANK_CORE.existing_facility f
              WHERE f.closed_at IS NULL

  - changeSet:
      id: 006-create-chat-loan-application
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.chat_v_loan_application AS
              SELECT
                la.application_id,
                la.customer_id,
                p.name AS product_name,
                p.product_type,
                p.currency,
                la.amount_requested,
                la.term_months,
                la.purpose,
                la.status,
                la.submitted_at
              FROM BANK_CORE.loan_application la
              JOIN BANK_CORE.product_catalog p ON p.product_id = la.product_id

  - changeSet:
      id: 006-create-chat-application-document
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.chat_v_application_document AS
              SELECT
                d.application_id,
                d.doc_id,
                d.requested_type,
                d.doc_type,
                d.ocr_status,
                d.ocr_confidence,
                d.quality_tier,
                d.uploaded_at
              FROM BANK_CORE.loan_application_document d

  # Broader views (RESEARCH_AGENT — read-only, broader scope)

  - changeSet:
      id: 006-create-research-full-transactions
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.research_v_full_transactions AS
              SELECT
                a.customer_id,
                t.account_id,
                t.txn_id,
                t.txn_date,
                t.amount,
                t.currency,
                t.txn_type,
                t.description,
                t.balance_after
              FROM BANK_CORE.account_transaction t
              JOIN BANK_CORE.account a ON a.account_id = t.account_id

  - changeSet:
      id: 006-create-research-decision-history
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.research_v_decision_history AS
              SELECT
                d.decision_id,
                d.application_id,
                d.human_outcome,
                d.human_user,
                d.decided_at,
                d.agent_recommendation,
                d.agent_run_id,
                d.computed_dti,
                d.computed_pti
              FROM BANK_CORE.decision d

  - changeSet:
      id: 006-create-research-decision-audit
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.research_v_decision_audit AS
              SELECT
                a.audit_id,
                a.agent_run_id,
                a.step_no,
                a.tool_name,
                a.status,
                a.started_at,
                a.ended_at,
                a.duration_ms
              FROM BANK_CORE.decision_audit a

  - changeSet:
      id: 006-create-research-research-audit
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.research_v_research_audit AS
              SELECT
                a.audit_id,
                a.research_run_id,
                a.hitl_task_id,
                a.reviewer,
                a.step_no,
                a.tool_name,
                a.status,
                a.started_at,
                a.ended_at,
                a.duration_ms
              FROM BANK_CORE.research_audit a

  - changeSet:
      id: 006-create-research-policy-parameter-history
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.research_v_policy_parameter_history AS
              SELECT
                h.hist_id,
                h.config_key,
                h.old_value,
                h.new_value,
                h.changed_at,
                h.changed_by,
                h.change_reason
              FROM BANK_CORE.policy_parameter_history h

  - changeSet:
      id: 006-create-research-hitl-task
      author: paf-poc
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.research_v_hitl_task AS
              SELECT
                t.task_id,
                t.application_id,
                t.assigned_to,
                t.state,
                t.agent_recommendation,
                t.agent_run_id,
                t.human_outcome,
                t.human_user,
                t.created_at,
                t.closed_at
              FROM BANK_CORE.hitl_task t

  # ORA-01720 fix: Oracle requires the view owner (BANK_VIEWS) to hold
  # WITH GRANT OPTION on the underlying base tables before a third
  # party (PAF_PLATFORM) can be granted SELECT on the view. The
  # earlier `006-grant-app-to-reporting` granted plain SELECT — kept
  # for migration history. This changeset re-grants WITH GRANT OPTION;
  # the second grant is additive in Oracle (it upgrades the existing
  # privilege rather than duplicating it).
  - changeSet:
      id: 006-grant-app-to-reporting-with-grant-option
      author: paf-poc
      changes:
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.customer TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.employment TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.account TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.account_transaction TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.credit_bureau_snapshot TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.existing_facility TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.loan_application TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.product_catalog TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.loan_application_document TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.decision TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.decision_audit TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.research_audit TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.policy_parameter_history TO BANK_VIEWS WITH GRANT OPTION
        - sql:
            sql: |-
              GRANT SELECT ON BANK_CORE.hitl_task TO BANK_VIEWS WITH GRANT OPTION

  # Add kyc_updated_at to the customer-safe applicant profile view.
  - changeSet:
      id: 006-chat-applicant-profile-add-kyc-updated-at
      author: paf-poc
      runOnChange: true
      changes:
        - sql:
            sql: |-
              CREATE OR REPLACE VIEW BANK_VIEWS.chat_v_applicant_profile AS
              SELECT
                c.customer_id,
                c.full_name,
                c.date_of_birth,
                TRUNC(MONTHS_BETWEEN(SYSDATE, c.date_of_birth) / 12) AS age_years,
                c.residency,
                c.kyc_status,
                c.kyc_updated_at,
                e.employer_name,
                e.employment_type,
                e.monthly_salary,
                e.tenure_months
              FROM BANK_CORE.customer c
              LEFT JOIN BANK_CORE.employment e
                ON e.customer_id = c.customer_id
                AND e.is_current = 'Y'
